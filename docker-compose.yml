version: '3'
services:
  kafka:
    image: "spotify/kafka"
    ports:
    - "9092:9092"
    - "2181:2181"
  gateway:
    build:
      context: .
      dockerfile: ./gateway/Dockerfile
      args:
        BOOTSTRAP_SERVERS: kafka:9092
    ports:
      - "8080:8080"
    links:
    - kafka